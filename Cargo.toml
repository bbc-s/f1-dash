[workspace]
members = ["shared", "realtime", "api", "archive", "simulator", "signalr"]
default-members = ["realtime", "api", "archive"]
resolver = "2"

[workspace.dependencies]
shared = { path = "shared" }
signalr = { path = "signalr" }
